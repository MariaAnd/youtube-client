import { HighlightPublicationDateDirective } from './highlight-publication-date.directive';

describe('HighlightPublicationDateDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPublicationDateDirective();
    expect(directive).toBeTruthy();
  });
});
