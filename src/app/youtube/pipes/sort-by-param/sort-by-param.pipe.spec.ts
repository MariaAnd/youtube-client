import { SortByParamPipe } from './sort-by-param.pipe';

describe('SortByParamPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByParamPipe();
    expect(pipe).toBeTruthy();
  });
});
